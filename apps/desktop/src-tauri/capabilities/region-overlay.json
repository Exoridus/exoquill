{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "region-overlay",
  "description": "Capability for the region-OCR selection overlay window: events to forward the result, and closing itself.",
  "windows": ["region-overlay"],
  "permissions": ["core:default", "core:window:allow-close"]
}
