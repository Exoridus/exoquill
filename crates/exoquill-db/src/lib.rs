//! SQLite persistence layer for ExoQuill.
//!
//! Owns the schema and migrations for `notes`, `note_events`, `settings`,
//! `models`, `captures` and `jobs` (see `docs/roadmap.md`, PR 1 & PR 2, and the
//! data model in the product spec).
